

## Localhost
```
cp wrangler.toml.example wrangler.toml
# 編輯wrangler.toml，填入參數
npm install
npm run dev
```

## docker compose
```
cp wrangler.toml.example wrangler.toml
# 編輯wrangler.toml，填入參數
docker-compose up -d 
```